# Roadmap

The executable, numbered roadmap with status, dependencies, verification, and release gates is [MASTER_PLAN.md](MASTER_PLAN.md). Start at the first eligible incomplete item. The initial path is: source provenance → repeatable build and tests → compare recovered release → auth/data inventory → one measured workflow improvement → controlled staging → production release.
